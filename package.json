{
  "name": "lumiverse-greeting-image-generator",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/frontend.ts --target browser --outfile dist/frontend.js && bun build src/backend.ts --target bun --outfile dist/backend.js",
    "typecheck": "tsc --noEmit",
    "test": "bun test tests",
    "preview": "bun scripts/preview.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "lumiverse-spindle-types": "0.6.31",
    "typescript": "^5.9.3",
    "happy-dom": "^20.0.0"
  }
}
